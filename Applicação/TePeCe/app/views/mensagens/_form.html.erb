<%= form_for @mensagen, url: new_user_mensagen_path(@user), html: { method: :put } do |f| %>
  <% if @mensagen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mensagen.errors.count, "error") %> prohibited this mensagen from being saved:</h2>

      <ul>
      <% @mensagen.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
    <div class="row">
	<div class="span3 offset1">
  
  <div class="field">
    <%= f.label :destinatario %><br>
    <%= f.collection_select :destinatario, @users, :id, :user_name %>
  </div>
  <div class="field">
    <%= f.label :assunto %><br>
    <%= f.text_field :assunto %>
  </div>
  <div class="field">
    <%= f.label :mensagem %><br>
    <%= f.text_area :mensagem,  placeholder: "Nova Mensagem..." %>
  </div>
  <div class="actions">
    <%= f.submit "Enviar", class: "btn btn-primary" %>
  </div>
  
   </div>
  </div>
<% end %>
